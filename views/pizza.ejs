<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head", {estilos: ["/css/pizza.css"], titulo: "Pizza | Pizzaria"}) %>
  <body>
    <header>
      <a href="/">
        <img
          src="/img/logo.svg"
          alt="Logo da Pizzaria Fantástica"
          width="64"
          height="64"
        />
      </a>
      <input
        type="text"
        name=""
        id="campo-de-busca"
        placeholder="Qual pizza você procura?"
      />
      <button class="btBuscar">Buscar</button>
      <a href="/carrinho">
        <img src="" alt="" />
        <span>0</span>
      </a>
      <a href="">
        <img src="" alt="" />
      </a>
    </header>
    <main>
      <h1><%= pizza.nome %></h1>
      <h2><%= pizza.preco.toLocaleString('pt-br',{style: 'currency', currency: 'BRL'}) %></h2>
      <img src="<%= pizzaImg %>" alt="<%= pizza.nome %>" />
      <div>Ingredientes: <%= pizza.ingredientes.join(", ") %></div>
      <form action="/pizzas/addCart" method="post">
        <input type="hidden" name="aEscolhida" value="<%= pizza.id %>" />
        <button>+ Add</button>
      </form>
      <a href="/pizzas/<%= pizza.id > 1 ? pizza.id - 1 : pizza.id %>" class="prev">&lt;</a>
      <a href="/pizzas/<%= pizza.id + 1 %>" class="next">&gt;</a>
    </main>
  </body>
</html>
